import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function CartPage() {
  console.log("[server] CartPage render");

  return (
    <RoutePlaceholder
      title="Giỏ hàng"
      description="Khung trang giỏ — store client sẽ gắn ở Day 17."
    />
  );
}
