import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function CheckoutPage() {
  console.log("[server] CheckoutPage render");

  return (
    <RoutePlaceholder
      title="Thanh toán"
      description="Khung checkout — form + Auth sẽ làm ở Day 17."
    />
  );
}
