# ShopLite (Next.js)

```bash
cp .env.example .env.local
npm install
npm run dev
```

Login demo: `thanhg@shoplite.com` / `shoplite123`

Deploy Vercel: Root Directory = `v5-next`. Cần `AUTH_SECRET` và `AUTH_URL`.
